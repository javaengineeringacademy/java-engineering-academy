/*
 * Exercise: Arrays in C
 * Difficulty: ★★☆☆☆ (2/5)
 * Learning Objectives:
 *   - Declare and initialize arrays
 *   - Access and modify array elements
 *   - Understand array memory layout
 *   - Practice array traversal and manipulation
 */

#include <stdio.h>

#define SIZE 5

int main(void) {
    /*
     * TODO 1: Declare and initialize an integer array with values {10, 20, 30, 40, 50}
     * Print each element using a for loop
     */

    /*
     * TODO 2: Create an array of 10 integers and fill it with
     * values from 1 to 10 using a for loop
     */

    /*
     * TODO 3: Write code to find the sum and average of array elements
     * Expected sum: 150, Expected average: 30.0
     */

    /*
     * TODO 4: Find the maximum and minimum values in an array
     * Test with: {3, 7, 1, 9, 4, 6, 2, 8, 5}
     */

    /*
     * TODO 5: Reverse an array in place
     * Original: {1, 2, 3, 4, 5}
     * Reversed: {5, 4, 3, 2, 1}
     */

    /*
     * TODO 6: Search for a value in an array (linear search)
     * Return the index if found, -1 if not found
     * Search for value 7 in: {2, 5, 7, 9, 11}
     */

    /*
     * TODO 7: Count the occurrences of a specific value in an array
     * Count occurrences of 3 in: {1, 3, 2, 3, 4, 3, 5}
     * Expected: 3
     */

    /*
     * TODO 8: Copy elements from one array to another
     * Verify the copy is successful by printing both arrays
     */

    /* Test cases */
    /*
    printf("=== Test Cases ===\n");
    printf("Array elements: ");
    print_array(arr, SIZE);
    printf("Sum: %d (expected: 150)\n", sum);
    printf("Average: %.1f (expected: 30.0)\n", average);
    printf("Max: %d (expected: 9)\n", max);
    printf("Min: %d (expected: 1)\n", min);
    printf("Reversed array: ");
    print_array(reversed, SIZE);
    printf("Index of 7: %d (expected: 2)\n", index);
    printf("Occurrences of 3: %d (expected: 3)\n", count);
    */

    return 0;
}

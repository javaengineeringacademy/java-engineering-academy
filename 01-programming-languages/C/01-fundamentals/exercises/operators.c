/*
 * Exercise: Operators in C
 * Difficulty: ★★☆☆☆ (2/5)
 * Learning Objectives:
 *   - Master arithmetic operators (+, -, *, /, %)
 *   - Understand relational operators (==, !=, <, >, <=, >=)
 *   - Practice logical operators (&&, ||, !)
 *   - Learn about assignment operators (+=, -=, *=, /=)
 */

#include <stdio.h>

int main(void) {
    int a = 10, b = 3;

    /*
     * TODO 1: Calculate and print the result of a + b
     * Expected output: 13
     */

    /*
     * TODO 2: Calculate and print the result of a - b
     * Expected output: 7
     */

    /*
     * TODO 3: Calculate and print the result of a * b
     * Expected output: 30
     */

    /*
     * TODO 4: Calculate and print the result of a / b (integer division)
     * Expected output: 3
     */

    /*
     * TODO 5: Calculate and print the result of a % b (modulo)
     * Expected output: 1
     */

    /*
     * TODO 6: Demonstrate compound assignment operators
     * Start with x = 10, then use: +=, -=, *=, /=, %=
     * Print the result after each operation
     */

    /*
     * TODO 7: Use relational operators to compare a and b
     * Print results of: a == b, a != b, a > b, a < b, a >= b, a <= b
     * (1 for true, 0 for false)
     */

    /*
     * TODO 8: Use logical operators with the following expressions:
     * - (a > 5) && (b < 5)   -- expected: 1
     * - (a < 5) || (b < 5)   -- expected: 1
     * - !(a == b)             -- expected: 1
     */

    /*
     * TODO 9: Demonstrate operator precedence
     * Calculate: 2 + 3 * 4 and (2 + 3) * 4
     * Print both results with clear labels
     */

    /* Test cases */
    /*
    printf("=== Test Cases ===\n");
    printf("Addition: %d (expected: 13)\n", a + b);
    printf("Subtraction: %d (expected: 7)\n", a - b);
    printf("Multiplication: %d (expected: 30)\n", a * b);
    printf("Division: %d (expected: 3)\n", a / b);
    printf("Modulo: %d (expected: 1)\n", a % b);
    printf("(2 + 3 * 4) = %d (expected: 14)\n", 2 + 3 * 4);
    printf("((2 + 3) * 4) = %d (expected: 20)\n", (2 + 3) * 4);
    */

    return 0;
}

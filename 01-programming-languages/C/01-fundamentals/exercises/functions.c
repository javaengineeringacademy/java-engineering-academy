/*
 * Exercise: Functions in C
 * Difficulty: ★★☆☆☆ (2/5)
 * Learning Objectives:
 *   - Define and call functions
 *   - Understand function parameters and return values
 *   - Practice function prototypes
 *   - Learn about local and global scope
 */

#include <stdio.h>

/* TODO: Add function prototypes here */
// Example: int add(int a, int b);

/*
 * TODO 1: Write a function called 'add' that takes two integers
 * and returns their sum
 */

/*
 * TODO 2: Write a function called 'is_even' that takes an integer
 * and returns 1 if even, 0 if odd
 */

/*
 * TODO 3: Write a function called 'factorial' that takes an integer
 * and returns its factorial (use iterative approach)
 * Example: factorial(5) = 120
 */

/*
 * TODO 4: Write a function called 'swap' that swaps two integers
 * using pointers (pass by reference)
 */

/*
 * TODO 5: Write a function called 'print_array' that takes an integer array
 * and its size, then prints all elements
 */

/*
 * TODO 6: Write a recursive function called 'fibonacci' that returns
 * the nth Fibonacci number
 * fibonacci(0) = 0, fibonacci(1) = 1, fibonacci(5) = 5
 */

/*
 * TODO 7: Write a function called 'find_max' that takes an array and size
 * and returns the maximum value
 */

/*
 * TODO 8: Write a function called 'reverse_string' that reverses a string
 * in place (modify the original string)
 */

int main(void) {
    /* Test cases */
    /*
    printf("=== Test Cases ===\n");

    // Test add
    printf("add(3, 4) = %d (expected: 7)\n", add(3, 4));

    // Test is_even
    printf("is_even(4) = %d (expected: 1)\n", is_even(4));
    printf("is_even(7) = %d (expected: 0)\n", is_even(7));

    // Test factorial
    printf("factorial(5) = %d (expected: 120)\n", factorial(5));

    // Test swap
    int x = 10, y = 20;
    swap(&x, &y);
    printf("After swap: x = %d, y = %d (expected: 20, 10)\n", x, y);

    // Test print_array
    int arr[] = {1, 2, 3, 4, 5};
    printf("Array: ");
    print_array(arr, 5);

    // Test fibonacci
    printf("fibonacci(6) = %d (expected: 8)\n", fibonacci(6));

    // Test find_max
    printf("Max in array: %d (expected: 5)\n", find_max(arr, 5));
    */

    return 0;
}

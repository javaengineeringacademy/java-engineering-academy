/*
 * Exercise: Variables in C
 * Difficulty: ★☆☆☆☆ (1/5)
 * Learning Objectives:
 *   - Understand variable declaration and initialization
 *   - Learn about different data types (int, float, double, char)
 *   - Practice variable naming conventions
 *   - Understand scope and lifetime of variables
 */

#include <stdio.h>

int main(void) {
    /*
     * TODO 1: Declare and initialize an integer variable named 'age' with value 25
     * Hint: Use the 'int' data type
     */

    /*
     * TODO 2: Declare and initialize a float variable named 'height' with value 5.9
     * Hint: Use the 'float' data type and suffix 'f'
     */

    /*
     * TODO 3: Declare and initialize a double variable named 'pi' with value 3.14159
     * Hint: Use the 'double' data type
     */

    /*
     * TODO 4: Declare and initialize a char variable named 'grade' with value 'A'
     * Hint: Use single quotes for char values
     */

    /*
     * TODO 5: Declare a constant integer variable named 'MAX_SIZE' with value 100
     * Hint: Use the 'const' keyword
     */

    /*
     * TODO 6: Print all variables using printf with appropriate format specifiers
     * Format specifiers: %d (int), %f (float), %lf (double), %c (char)
     * Example: printf("Age: %d\n", age);
     */

    /*
     * TODO 7: Demonstrate variable reassignment by changing 'age' to 30
     * and printing the new value
     */

    /* Test cases - uncomment to verify your implementation */
    /*
    printf("=== Test Cases ===\n");
    printf("Age: %d (expected: 25)\n", age);
    printf("Height: %.1f (expected: 5.9)\n", height);
    printf("Pi: %.5f (expected: 3.14159)\n", pi);
    printf("Grade: %c (expected: A)\n", grade);
    printf("MAX_SIZE: %d (expected: 100)\n", MAX_SIZE);
    printf("Age after reassignment: %d (expected: 30)\n", age);
    */

    return 0;
}

/*
 * Exercise: Strings in C
 * Difficulty: ★★☆☆☆ (2/5)
 * Learning Objectives:
 *   - Understand C strings as character arrays
 *   - Use string functions from string.h
 *   - Practice string manipulation
 *   - Learn about string literals vs character arrays
 */

#include <stdio.h>
#include <string.h>

int main(void) {
    /*
     * TODO 1: Declare and initialize a string variable with "Hello, World!"
     * Print its length using strlen()
     */

    /*
     * TODO 2: Write a function (or inline code) to count vowels in a string
     * Test with "Programming" - expected: 3 (o, a, i)
     */

    /*
     * TODO 3: Concatenate two strings using strcat()
     * String 1: "Hello, "
     * String 2: "World!"
     * Expected result: "Hello, World!"
     */

    /*
     * TODO 4: Compare two strings using strcmp()
     * Compare "abc" with "abd" - expected: negative value
     * Compare "abc" with "abc" - expected: 0
     */

    /*
     * TODO 5: Copy a string using strcpy()
     * Copy "Sample Text" to a new buffer
     */

    /*
     * TODO 6: Convert a string to uppercase
     * Test with "hello world" - expected: "HELLO WORLD"
     * Hint: Use toupper() from ctype.h
     */

    /*
     * TODO 7: Reverse a string in place
     * Test with "abcdef" - expected: "fedcba"
     */

    /*
     * TODO 8: Check if a string is a palindrome
     * Test with "racecar" - expected: 1 (true)
     * Test with "hello" - expected: 0 (false)
     */

    /*
     * TODO 9: Tokenize a string using strtok()
     * Tokenize "one,two,three,four" by comma
     * Print each token
     */

    /* Test cases */
    /*
    printf("=== Test Cases ===\n");
    printf("Length of '%s': %zu (expected: 13)\n", str, strlen(str));
    printf("Vowels in 'Programming': %d (expected: 3)\n", vowel_count);
    printf("Concatenated: '%s' (expected: 'Hello, World!')\n", result);
    printf("strcmp('abc', 'abd'): %d (expected: negative)\n", result);
    printf("Uppercase: '%s' (expected: 'HELLO WORLD')\n", upper);
    printf("Reversed: '%s' (expected: 'fedcba')\n", reversed);
    printf("'racecar' is palindrome: %d (expected: 1)\n", is_palindrome("racecar"));
    printf("'hello' is palindrome: %d (expected: 0)\n", is_palindrome("hello"));
    */

    return 0;
}

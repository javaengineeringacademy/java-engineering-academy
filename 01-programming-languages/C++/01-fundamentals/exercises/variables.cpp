/*
 * Exercise: Variables in C++
 * Difficulty: ★☆☆☆☆ (1/5)
 * Learning Objectives:
 *   - Understand variable declaration with auto keyword
 *   - Learn about const and constexpr
 *   - Practice reference variables
 *   - Understand type deduction and casting
 */

#include <iostream>
#include <string>
using namespace std;

int main() {
    /*
     * TODO 1: Declare variables using traditional types and auto keyword
     * int, double, float, char, bool, string
     */

    /*
     * TODO 2: Use auto for type deduction
     * auto x = 42;
     * auto y = 3.14;
     * auto z = "Hello";
     * Print the type of each using typeid
     */

    /*
     * TODO 3: Declare a const variable and a constexpr variable
     * const int MAX = 100;
     * constexpr double PI = 3.14159;
     * Print both values
     */

    /*
     * TODO 4: Demonstrate reference variables
     * int a = 10;
     * int &ref = a;
     * Modify a through ref and print both
     */

    /*
     * TODO 5: Show different types of initialization
     * Direct initialization: int x(10);
     * Copy initialization: int y = 10;
     * List initialization: int z{10};
     * Uniform initialization: int w = {10};
     */

    /*
     * TODO 6: Demonstrate scope and lifetime
     * Create variables in different scopes (global, local, block)
     */

    /*
     * TODO 7: Use sizeof to check sizes of different types
     * Print: int, long, float, double, char, bool
     */

    /* Test cases */
    /*
    cout << "=== Test Cases ===" << endl;
    cout << "Integer: " << x << " (expected: 42)" << endl;
    cout << "Double: " << y << " (expected: 3.14)" << endl;
    cout << "String: " << z << " (expected: Hello)" << endl;
    cout << "Const: " << MAX << " (expected: 100)" << endl;
    cout << "Constexpr: " << PI << " (expected: 3.14159)" << endl;
    cout << "Reference: " << ref << " (expected: 10)" << endl;
    cout << "sizeof(int): " << sizeof(int) << " (expected: 4)" << endl;
    cout << "sizeof(double): " << sizeof(double) << " (expected: 8)" << endl;
    */

    return 0;
}

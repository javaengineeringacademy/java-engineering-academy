# Solutions: Annotation Processing

See annotation definitions and processor notes in this directory.

## Exercise 1: Getter Generator
Processor that reads @GenerateGetters and generates getFieldName() for each field.

## Exercise 2: ToString Generator
Processor that generates toString() with all field values.

## Exercise 3: Immutable Builder
Processor that generates a Builder with final fields and a build() method.

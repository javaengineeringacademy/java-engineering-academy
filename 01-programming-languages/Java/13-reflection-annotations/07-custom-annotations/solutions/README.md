# Solutions: Custom Annotations

See solution files in this directory.

## Exercise 1: Validation Annotation
@NotEmpty annotation with a validator that checks String fields for null/empty.

## Exercise 2: Cache Annotation
@Cacheable annotation with a method that reads cache TTL configuration.

## Exercise 3: Metadata Extractor
@ApiEndpoint annotation with a method that extracts all values into a Map.

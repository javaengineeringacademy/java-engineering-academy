# Iterator

## What
Object to traverse collection, can remove elements.

## Methods
- hasNext() - check if more elements
- next() - get next element
- remove() - remove last returned element

## When
- Need to remove during iteration
- Forward-only traversal

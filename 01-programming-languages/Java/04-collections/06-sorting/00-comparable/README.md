# Comparable

## What
Natural ordering interface. Class implements it to define default sort.

## Method
int compareTo(T o) - negative if less, 0 if equal, positive if greater

## Usage
Collections.sort(list) or list.sort(null)

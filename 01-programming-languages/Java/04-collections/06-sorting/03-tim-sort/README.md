# TimSort

## What
Hybrid sorting algorithm (merge sort + insertion sort).
Default sorting algorithm in Java.

## Complexity
- Best: O(n)
- Average: O(n log n)
- Worst: O(n log n)
- Space: O(n)

## Why
Adaptive to partially sorted data. Very efficient for real-world data.

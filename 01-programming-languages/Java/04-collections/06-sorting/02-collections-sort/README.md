# Collections.sort()

## What
Utility method to sort lists.

## Variants
- Collections.sort(list) - natural order
- Collections.sort(list, comparator) - custom order

## Usage
```java
Collections.sort(list);
Collections.sort(list, Comparator.reverseOrder());
```
